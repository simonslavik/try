# Test Environment Variables
NODE_ENV=test
PORT=3002

# Test Database - use Docker Compose database for now
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/books_service"

# JWT Secret for testing
JWT_SECRET="test-secret-key-for-testing-only"

# Google Books API (use real key or mock)
GOOGLE_BOOKS_API_KEY="test-api-key"
