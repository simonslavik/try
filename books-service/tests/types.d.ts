/// <reference types="jest" />

// eslint-disable-next-line @typescript-eslint/no-namespace
declare global {
  // eslint-disable-next-line no-var
  var testUserId: string;
  // eslint-disable-next-line no-var
  var testBookClubId: string;
}

export {};
